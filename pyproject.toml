[project]
name = "gemini-search"
version = "0.1.0"
description = "Google Search via Gemini Grounding API + Deep Research"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-genai>=1.67.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-mock>=3.0",
]
